jQuery(document).ready(function ($) {

	// $('.base-attribute-select select').select2();
	// $('.conditional-step-select select').select2();
	// $('.conditional-input-select select').select2();
	// $('.conditional-repeater-attribute-select select').select2();

	// $(document).on('click', '.cf-complex__group-actions button:last-of-type', function() {
	// 	if ( ! $(this).parents('.cf-complex__group').hasClass('.cf-complex__group--collapsed') ) {
	// 		$('.base-attribute-select select').select2({ width: 'resolve' });
	// 	}
	// });
	
	// $(document).on('click', '.cf-complex__actions .cf-complex__toggler', function() {
	// 	$(this).closest('.cf-field__body').find('> .cf-complex__groups > .cf-complex__group').each(function(group,index){
	// 		if ( ! $(group).hasClass('.cf-complex__group--collapsed') ) {
	// 			$('.base-attribute-select select').select2({ width: 'resolve' });
	// 		}
	// 	});
	// });

	// $(document).on('newComplexGroupAdded', function() {
	// 	setTimeout(function() {

	// 		$('.base-attribute-select select').select2();
	// 		$('.conditional-step-select select').select2();
			
	// }, 10);
	// });

});
